#include "types.h"
#include "user.h"
#include "date.h"

int
main ( int argc , char * argv [])
{
int res = deadline(atoi(argv[1]), atoi(argv[2]));
// printf(1, "exec_time: %d", res);
// return res;
exit ();
}